// project import
import botbuilder from "./bot-builder";
import chatWidget from "./chat-widget";

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [botbuilder, chatWidget]
};

export default menuItems;
